---
- name: Exercise 1b
  hosts: localhost
  gather_facts: False
  tasks:
    - name: Loop over four IP addresses
      ansible.builtin.include_tasks: "{{ file }}"
      loop:
        - 10.0.0.1
        - 10.0.0.2
        - 10.0.0.3
        - 10.0.0.4
