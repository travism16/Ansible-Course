---
- name: Configure IOS-XE device with DNS, NTP and Domain Name
  cisco.ios.ios_config:
    lines:
      - "ip domain name {{ domain_name }}"
      - "ntp server {{ ntp_server1 }}"
      - "ntp server {{ ntp_server2 }}"
      - "ip name-server {{ dns_server1 }} {{ dns_server2 }}"
